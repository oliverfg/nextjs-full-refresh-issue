# nextjs-full-refresh-issue
First install the dependencies with the command:
```
npm install
```

To up the both projects use the command
```
npm run dev-fashion
```

Then acces to url http://localhost:3000/hfm/ and the infinite loop can be observed.
